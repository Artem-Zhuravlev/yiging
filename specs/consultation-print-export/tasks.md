# Tasks — Consultation Print / PDF Export (SPEC-027)

Each task references the requirement(s) it fulfills. Mark done only once the linked test passes.

| Task ID         | Description                                                              | Requirement(s)          | Test(s)                             | Status |
| ----------------- | ------------------------------------------------------------------------------ | --------------------------- | ---------------------------------------- | ------ |
| TASK-PRINT-001 | Add "Print / Export" button + `print:hidden` classes to `ConsultationPage.vue` and `App.vue` | REQ-PRINT-001, 002, 003, 004, 006 | `ConsultationPage.spec.ts`       | done   |
| TASK-PRINT-002 | Run `npm run verify`, manually verify print preview against real running API/UI, update README + specs/README | acceptance criteria | `npm run verify`, manual | done   |
