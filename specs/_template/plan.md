# Plan — <Feature Name> (SPEC-NNN)

**Depends on spec status:** `approved`

## Technical approach

High-level description of how this will be built.

## Architecture decisions

Key choices and why (e.g. "domain logic lives in yijing-core, not the controller, because...").

## Affected areas

- `packages/yijing-core/...`
- `apps/api/src/...`
- `apps/web/src/...`

## Data / schema changes

New tables, migrations, or domain types.

## Risks / open questions

Anything that could change the plan once implementation starts.
