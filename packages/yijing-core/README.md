# yijing/core

Pure I Ching domain model. No framework, HTTP, database, filesystem, or AI dependencies allowed here — see [`specs/domain-model`](../../specs/domain-model/spec.md) (SPEC-002).

Implementation begins once SPEC-002 is `approved`. This package currently contains scaffolding only.
